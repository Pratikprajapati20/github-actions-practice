name: Hello
'on':
  push:
    branches:
      - main
jobs:
  greet:
     #This is github runner
    runs-on: ubuntu-latest
    steps:
      - name: sayhello to users
        run: echo "Hello dosto"
      
      - name: say hello to username
        run: echo "Hello $USER"
